<?php
require_once plugin_dir_path(__FILE__) . '../../infra/repositories/index.php';

class List{{pascalCase moduleName}}UseCase {
  function __construct() {
    $this->repository = new {{pascalCase moduleName}}Repository();
  }

  function execute() {    
    return array('{{moduleName}}' => $this->repository->list());
  }
}